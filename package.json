{
  "name": "agent-auth",
  "version": "0.1.0",
  "private": true,
  "description": "AgentAuth - OAuth-like authentication for AI agents with onchain sybil resistance",
  "workspaces": [
    "server",
    "sdk",
    "demo",
    "agent-skill"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server",
    "dev:demo": "npm run dev --workspace=demo",
    "build": "echo 'Vercel build: API is api/index.js, static files in public/'",
    "build:local": "npm run build --workspace=sdk && npm run build --workspace=server"
  },
  "license": "MIT"
}
